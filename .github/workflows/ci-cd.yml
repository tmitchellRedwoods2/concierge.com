name: CI/CD Pipeline

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  test:
    runs-on: ubuntu-latest
    
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.12'
    
    - name: Cache pip dependencies
      uses: actions/cache@v3
      with:
        path: ~/.cache/pip
        key: ${{ runner.os }}-pip-${{ hashFiles('**/requirements*.txt') }}
        restore-keys: |
          ${{ runner.os }}-pip-
    
    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install -r requirements.txt
        pip install -r requirements-test.txt
        pip install -r requirements-selenium.txt
    
    - name: Install Chrome for Selenium
      run: |
        sudo apt-get update
        sudo apt-get install -y chromium-browser chromium-chromedriver
    
    - name: Run unit tests
      run: |
        python -m pytest tests/test_*.py -v --cov=. --cov-report=xml
    
    - name: Run Selenium tests against deployed app
      run: |
        # Wait a moment for any recent deployments
        sleep 5
        # Run smoke tests against the deployed Streamlit Cloud app
        python run_selenium_tests.py --smoke-only --url "https://concierge-com.streamlit.app"
    
    - name: Upload coverage to Codecov
      uses: codecov/codecov-action@v3
      with:
        file: ./coverage.xml
        flags: unittests
        name: codecov-umbrella
        fail_ci_if_error: false

  deploy:
    needs: test
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
    - name: Deployment Notification
      run: |
        echo "🚀 Deployment to Streamlit Cloud triggered successfully!"
        echo "✅ All unit tests passed"
        echo "📊 Test coverage report generated"
        echo "🌐 Streamlit Cloud will automatically deploy the latest changes"
